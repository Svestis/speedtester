# No third-party Python packages needed. speedtester.py and report.py use
# only the standard library, plus the Ookla Speedtest CLI (a system binary,
# not a pip package) - see README for installation.
